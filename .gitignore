# ─── Python ───────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
.Python

# ─── Dados do dashboard ───────────────────────────────────────
# dashboard_data.json É commitado intencionalmente.
# Motivo: Railway usa --so-servir e serve este JSON sem rebuscar dados,
# garantindo que Railway e PC local mostrem exatamente os mesmos números
# (retorno total com dividendos via yfinance).
# Para atualizar o Railway, rode: ./atualizar_railway.sh

# Cache HTTP do brapi.dev (sqlite local, pode crescer)
http_cache.sqlite
*.sqlite
pipeline_outputs/

# ─── Pasta de arquivos históricos (snapshots antigos) ─────
arquivo/

# ─── PDFs gerados ─────────────────────────────────────────
factsheet_*.pdf

# ─── Fonts baixadas automaticamente ──────────────────────
fonts/

# ─── Arquivos temporários do sistema e de editor ──────────
*.tmp
*.temp
*.bak
~$*
.~lock.*#
*.swp
*.swo
*~

# ─── Sistema operacional ──────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini
ehthumbs.db

# ─── IDEs e editores ──────────────────────────────────────
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# ─── Segredos (nunca commitar) ────────────────────────────
.env
.env.local
secrets.py
config_local.py

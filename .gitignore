# ─── Python ───────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
.Python

# ─── Dados gerados (recriados automaticamente pelo serve.py) ──
# Cada PC gera o seu próprio dashboard_data.json via `python serve.py`.
# O Railway também regenera na inicialização via --sem-browser.
# Não commitar: evita conflitos entre máquinas e diffs desnecessários.
dashboard_data.json

# Cache HTTP do brapi.dev (sqlite local, pode crescer)
http_cache.sqlite
*.sqlite
pipeline_outputs/

# ─── PDFs gerados ─────────────────────────────────────────
factsheet_*.pdf

# ─── Fonts baixadas automaticamente ──────────────────────
fonts/

# ─── Arquivos temporários do sistema e de editor ──────────
*.tmp
*.temp
*.bak
~$*
.~lock.*#
*.swp
*.swo
*~

# ─── Sistema operacional ──────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini
ehthumbs.db

# ─── IDEs e editores ──────────────────────────────────────
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# ─── Segredos (nunca commitar) ────────────────────────────
.env
.env.local
secrets.py
config_local.py
